# Test Environment Configuration
# For integration and unit tests

app:
  name: "Promenade CRM"
  environment: "test"
  version: "0.1.0"

server:
  host: "localhost"
  port: 8082
  read_timeout: 5s
  write_timeout: 5s

database:
  host: "localhost"
  port: 5433
  user: "system"
  password: "passw0rd"
  database: "promenade_test"
  ssl_mode: "disable"

logging:
  level: "info"
  format: "text"
  add_source: false
